/*
 * The configuration file for the interpreter.
 */
{
  "INTERPRETER_CONFIG": {
    "SUPPORTED_DATA_TYPES" : [ "INTEGER" ],
    "SUPPORTED_OPS": [ "PLUS", "MINUS", "DIVISION", "MULTIPLICATION" ],
    "MISC" :  ["EOL"]
  }
}
